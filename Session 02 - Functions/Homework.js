/*   Functions
 *
 *   Provide 4 Functions.
 *
 *   The Functions should do 1 of each:
 *    - return a static string 
 *    - return the argument that was passed into it.
 *    - return a static string concatinated with the argument that was passed into it.
 *    - return the additive results of two arguments passed into it.
 *
 *   Remember to "console.log()" you work so that you can see the results.
 *
 */
 
 